# @nocobase/app-websocket

Shared WebSocket contract and Node adapter for NocoBase application hosts and
servers. The Node adapter uses `ws` in `noServer` mode so an existing HTTP
server can route upgrade requests before accepting a connection.
