# App Access Control

Shared App-level member, role, and permission management for NocoBase 3.

The plugin owns the common management UI and server-side access-control model. An App supplies its role and business-resource definitions and keeps the resulting data in its own database.
