# Legacy client snapshot

This directory is a temporary, read-only reference for the `app-client`
migration. Do not add features here and do not import it from the new client or
from `@nocobase/app-client`.

It is excluded from checks and package publication. Delete the directory after
the new client has reached functional parity.
