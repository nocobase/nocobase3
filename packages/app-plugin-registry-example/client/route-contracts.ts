export interface RegistryExampleRouteIds {
  readonly index: string;
}

export const REGISTRY_EXAMPLE_ROUTE_IDS: RegistryExampleRouteIds =
  Object.freeze({
    index: '@nocobase/app-plugin-registry-example:index',
  });
