import { describe, expect, it } from "vitest";
import { createPublicFilesConfig, parseFilesConfig } from "../src/config/index.ts";
const input = (extra = {}) => ({ defaultPolicy: "p", backends: { b: { driver: "local", root: "/x", signingSecret: "s".repeat(32) } }, policies: { p: { backend: "b", description: "P", maxSize: 10, allowedContentTypes: ["image/*", "image/*"], uploadUrlTtlSeconds: 10, defaultReadUrlTtlSeconds: 10, maxReadUrlTtlSeconds: 20 } }, ...extra });
describe("G04 config", () => { it("parses and normalizes", () => expect(parseFilesConfig(input()).policies.p.allowedContentTypes).toEqual(["image/*"])); it("rejects invalid config", () => { expect(() => parseFilesConfig(input({ defaultPolicy: "x" }))).toThrow(); expect(() => parseFilesConfig(input({ policies: { p: { ...input().policies.p, allowedContentTypes: [".*"] } } }))).toThrow(); }); it("projects without secrets", () => { const out = JSON.stringify(createPublicFilesConfig(parseFilesConfig(input()), [{ uploadModes: ["presigned-put"] }, { uploadModes: ["proxy", "presigned-put"] }])); expect(out).not.toContain("s".repeat(32)); expect(out).toContain("proxy"); }); });
