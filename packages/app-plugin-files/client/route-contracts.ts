export interface FilesRouteIds {
  readonly demo: '@nocobase/app-plugin-files:demo';
}

export const FILES_ROUTE_IDS: Readonly<FilesRouteIds> = Object.freeze({
  demo: '@nocobase/app-plugin-files:demo',
});
