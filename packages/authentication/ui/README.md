# Authentication UI Registry

This directory contains application UI source intended to be copied into an
application's registry or client source. It is not part of the runtime
package export. The initial built-in UI is the fixed email/password flow.
