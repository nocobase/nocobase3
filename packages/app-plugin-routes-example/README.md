# @nocobase/app-plugin-routes-example

Routes app plugin example. When enabled, the convention-based
`server/routes/index.ts` entry mounts a simple `GET /routes-example` endpoint
without depending on application services.
