interface:
  display_name: 'NocoBase 3 Workflow Manage'
  short_description: 'Author, invoke, inspect, and diagnose NocoBase workflows'
  default_prompt: 'Use $nocobase3-workflow-manage to define, validate, invoke, inspect, or diagnose a NocoBase 3 workflow using the current app-plugin-workflow contracts.'

policy:
  allow_implicit_invocation: true
