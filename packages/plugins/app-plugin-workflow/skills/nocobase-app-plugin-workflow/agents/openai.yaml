interface:
  display_name: 'NocoBase App Plugin Workflow'
  short_description: 'Choose and build durable NocoBase business workflows'
  default_prompt: 'Use $nocobase-app-plugin-workflow to decide whether these business rules need Workflow, typed code, or both, then implement and verify the supported design.'

policy:
  allow_implicit_invocation: true
