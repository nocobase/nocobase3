# @nocobase/app-plugin-ai-knowledge-base

App-owned AI knowledge-base compatibility plugin. It exposes the legacy action contract under `/v2/api`, persists document segments and shards, registers AI Manager features, and provides the built-in PGVector provider.
