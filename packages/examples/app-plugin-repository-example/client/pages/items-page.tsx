import type { ReactElement } from 'react';
import { RepositoryPage } from './repository-page.js';
export default function Page(): ReactElement {
  return <RepositoryPage title='items' entityKey='items' />;
}
