# Sales example database

One migration creates sales membership, teams, team membership, projects, quotes with preparers, and orders. One transactional seed creates the final fictional accounts, records, permission sets, direct and team assignments, and default access, sharing and restriction rules. It runs through the application seed lifecycle and does not run from a request handler. Development-version upgrade migrations and seeds are not shipped.
