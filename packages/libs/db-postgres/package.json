{
  "name": "@nocobase/db-postgres",
  "version": "0.0.1",
  "description": "PostgreSQL dialect for @nocobase/db.",
  "type": "module",
  "prettier": "@nocobase/dev-config/prettier",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public",
    "exports": {
      ".": {
        "types": "./dist/src/index.d.ts",
        "import": "./dist/src/index.js"
      }
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run tests --exclude **/tests/integration/**",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "test:integration": "node --import tsx ./scripts/test-integration.ts"
  },
  "peerDependencies": {
    "@nocobase/db": "workspace:^"
  },
  "dependencies": {
    "knex": "catalog:",
    "pg": "^8.23.0",
    "pg-query-stream": "^4.14.0"
  },
  "devDependencies": {
    "@nocobase/db": "workspace:*",
    "@nocobase/db-testkit": "workspace:*",
    "@nocobase/dev-config": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
