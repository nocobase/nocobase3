{
  "name": "@nocobase/db",
  "version": "1.0.0-beta.2",
  "type": "module",
  "prettier": "@nocobase/dev-config/prettier",
  "engines": {
    "node": ">=24.0.0"
  },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "exports": {
      ".": {
        "types": "./dist/src/index.d.ts",
        "import": "./dist/src/index.js"
      }
    },
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "example": "tsx ./examples/command.ts",
    "playground": "tsx ./playground/command.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:db:up": "docker compose up -d postgres mysql",
    "test:db:up:oracle": "docker compose up -d oracle",
    "test:db:up:mssql": "docker compose up -d mssql && docker compose run --rm mssql-init",
    "test:db:down": "docker compose down -v",
    "test:integration": "vitest run tests/integration",
    "test:integration:all": "INTEGRATION_DB_CONNECTIONS=all vitest run tests/integration",
    "test:integration:oracle": "INTEGRATION_DB_CONNECTIONS=oracle vitest run tests/integration",
    "test:integration:mssql": "INTEGRATION_DB_CONNECTIONS=mssql vitest run tests/integration",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:examples": "tsc -p examples/tsconfig.json --noEmit",
    "typecheck:playground": "tsc -p playground/tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix --max-warnings 0",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "check": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm typecheck:examples && pnpm typecheck:playground && pnpm test && pnpm build",
    "fix": "pnpm lint:fix && pnpm format"
  },
  "dependencies": {
    "@nocobase/service-provider": "workspace:^",
    "knex": "catalog:"
  },
  "devDependencies": {
    "@hono/node-server": "catalog:",
    "@nocobase/dev-config": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "better-sqlite3": "catalog:",
    "eslint": "catalog:",
    "hono": "catalog:",
    "mysql2": "^3.23.3",
    "oracledb": "catalog:",
    "pg": "^8.23.0",
    "tedious": "catalog:",
    "tsx": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
