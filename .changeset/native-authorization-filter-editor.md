---
'@nocobase/app-plugin-authorization': patch
---

Unify custom record-scope editing around native DB filter nodes, with nested AND/OR groups, typed scalar values, and consistent empty-group validation across permission sets and access rules.
