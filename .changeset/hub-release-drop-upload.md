---
"@nocobase/app-plugin-hub": patch
---

Accept a dragged release artifact in the upload dialog, highlight the drop zone while a file is over it, and claim file drops that miss the zone so the browser does not open the artifact instead of uploading it.
