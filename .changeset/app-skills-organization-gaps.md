---
'@nocobase/app-skills': patch
---

Fill gaps in the organisation dimension reference found while building the departments example: the plugin layout, the complete organisation service, mapping `require` denials to 403, matching a list of ids in a record access, answering an empty result when a data scope selects nothing, creating demonstration accounts outside seeds, and starting a real application for the test matrix.
