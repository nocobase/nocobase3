---
'@nocobase/db': patch
---

Persist declared scalar field types in metadata, add temporal and floating-point Builder helpers, and define explicit temporal schema mappings. Validate final metadata for combined field alterations and preserve schema management permission checks before metadata writes.
