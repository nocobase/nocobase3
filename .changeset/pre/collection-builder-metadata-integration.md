---
'@nocobase/db': minor
---

Integrate Collection Builder with resolved Collections and supplemental Metadata documents, invalidate Collection Registry entries after physical Schema changes, and reject non-atomic renames or read-only Metadata writes before DDL.
