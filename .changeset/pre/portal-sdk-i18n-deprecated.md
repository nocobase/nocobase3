---
'@nocobase/app-portal-sdk': patch
---

Mark the Portal i18n runtime deprecated in favour of `@nocobase/i18n`. It keeps its own i18next instance, so the two must not be mixed inside one application.
