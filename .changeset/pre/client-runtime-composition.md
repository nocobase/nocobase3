---
'@nocobase/app-client': minor
'@nocobase/app-template-default': patch
---

Add the shared Client Runtime definition and resolution API, and simplify the default template startup into runtime resolution, application creation, and rendering stages.
