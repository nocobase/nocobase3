---
'@nocobase/app-plugin-scheduler': patch
---

Build the scheduled task list and detail pages from the same shared page sections, table, badge, and button components the other modules use, instead of page-local table markup and status chips.
