---
'@nocobase/i18n': patch
---

Derive a locale's type from the source locale with `LocaleResource`, so a package states its structure once instead of maintaining an interface alongside the wording it describes.
