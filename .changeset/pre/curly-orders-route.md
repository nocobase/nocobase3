---
'@nocobase/app-server': minor
---

Add defineRepositoryApiRoutes to expose explicitly configured Collection Repository methods through the api-client HTTP protocol, with JSON request validation, bounded list queries, and Repository error mapping. This basic adapter does not install authentication or authorization.
