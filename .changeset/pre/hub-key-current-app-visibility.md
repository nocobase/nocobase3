---
'@nocobase/app-plugin-hub': patch
---

Filter publishing API Key application metadata through the current user's read permissions after role changes, while preserving key revocation and stored bindings.
