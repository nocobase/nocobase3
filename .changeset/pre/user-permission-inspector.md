---
'@nocobase/authorization': patch
'@nocobase/app-plugin-authorization': minor
---

Add a paginated subject permission overview with grouped resource matrices, scope and field details, and structured authorization reasons. Reuse built-in rule lists within each authorization scope to avoid repeated database reads during batch inspection.
