---
'@nocobase/app-plugin-workflow': patch
---

Keep the Workflow CLI available from its TypeScript entry in an unbuilt source workspace while published installations continue to execute compiled output, and expose its checker errors through the public build entry.
