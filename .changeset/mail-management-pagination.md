---
'@nocobase/app-plugin-mail': patch
---

Replace the mail management load-more list with previous and next page navigation and a page size selector defaulting to 20 messages with 50 and 100 message options, clear selections when changing pages, and retain the current page when refreshing batch action results.
