---
'@nocobase/app-plugin-authorization': patch
---

Display settings permissions as grouped module rows with individually labeled action toggles and module, group, and filtered bulk selection. Keep database permissions in the existing matrix.
