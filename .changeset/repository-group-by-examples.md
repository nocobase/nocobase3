---
"@nocobase/app-plugin-repository-example": patch
---

Extend the aggregate page with customer order ranking, customer/status and product/price grouping examples. Add interactive row-count HAVING filters, readable relation links and per-panel request/result traces using the existing seed data.
