---
'@nocobase/create-hub': minor
---

Add `@nocobase/create-hub`, which scaffolds a standalone Hub with `pnpm create @nocobase/hub <directory>`. It downloads the published production Hub, generates secure local configuration and runtime directories, and installs dependencies unless `--no-install` is used.
