---
'@nocobase/app-skills': patch
---

The `nocobase-deployment` Skill now tells a Hub project, whose source changes, apart from an unmodified Hub, and points the latter at the published Docker image or at `@nocobase/hub-installer` and its global `nocobase-hub-installer` Skill.
