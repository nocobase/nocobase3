---
"@nocobase/app-plugin-hub": patch
---

Add `@nocobase/app-cli` as a development dependency so the plugin's tests can publish a Release through the real CLI against a real App Host. No runtime change.
