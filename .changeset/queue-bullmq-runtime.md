---
'@nocobase/queue': patch
---

Pin the verified BullMQ 6.3.6 runtime for the queue refactor and declare PostgreSQL as an optional peer, preserving existing queue dependencies during consumer migration.
