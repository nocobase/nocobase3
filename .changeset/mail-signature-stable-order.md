---
'@nocobase/app-plugin-mail': patch
---

Keep mail signatures in a stable name order when changing the default signature, including after reloading the list.

Use a compact text button for the action that sets the default signature.
