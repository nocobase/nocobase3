---
"@nocobase/app-plugin-authorization": patch
---

Declare built-in administration resources with the authorization fluent builder and separate their registration and record-access validation from application composition. Remove redundant page-action validation already enforced by the resource registry.
