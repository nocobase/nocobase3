---
'@nocobase/app-plugin-file': minor
---

Add an authenticated, read-only Files Settings page for standard database-backed file routes. Sources are registered automatically by `createFileRoute()` and listed with paginated file metadata without exposing storage object keys.
