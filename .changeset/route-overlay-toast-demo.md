---
'@nocobase/app-template-examples': patch
---

The route dialog and drawer examples gain a "Show a notification" button that raises a toast from inside the overlay, showing that notifications stay above dialogs and drawers.
