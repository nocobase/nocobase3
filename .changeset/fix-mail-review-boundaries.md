---
'@nocobase/app-plugin-mail': patch
---

Preserve messages across IMAP folder pagination and select each mailbox before incremental fetching. Retain local draft attachment contents when sending, scope the Mail client to its application container, bind public component translations to the Mail namespace, and narrow the Server entry point to supported integration contracts.
