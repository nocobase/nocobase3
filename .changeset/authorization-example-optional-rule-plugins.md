---
'@nocobase/app-plugin-authorization-example': patch
---

Default access, sharing rules and restriction rules are now optional peers. The seed skips each rule plugin's rows when its Collection does not exist, so the example installs and runs with the authorization plugin alone.
