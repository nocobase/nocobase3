---
'@nocobase/app-template-examples': patch
---

Register the departments example after the authorization example; it shows permission sets inherited through a department tree. The authorization example's migration and seed were edited in place, so reset an existing examples database before upgrading.
