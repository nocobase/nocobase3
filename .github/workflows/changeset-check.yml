name: Changeset Check

# 两层检查，严格程度不同：
#   validation —— 已提交的 changeset 本身有错（非法 YAML、未知包名、非法 bump）
#                 时阻止合并。这类问题机器能确定判断。
#   advisory   —— 改了可发布代码却没写 changeset 时提醒，但始终以成功退出。
#                 patch/minor/major 的取舍需要人判断，CI 猜不了。
#
# 这里不检查 .changeset/pre.json —— develop 常驻 pre 模式，它出现在指向
# develop 的 PR 里是正常的。保护 main 不被污染是 guard-main.yml 的职责，
# 那条只在目标分支为 main 的 PR 上运行。

on:
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read
  pull-requests: write

jobs:
  check:
    name: Validate and advise
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate changeset files
        shell: bash
        run: node scripts/validate-changesets.mjs

      - name: Advise on missing changesets
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          SKIP_LABEL: ${{ contains(github.event.pull_request.labels.*.name, 'release:skip') }}
        run: node scripts/advise-changesets.mjs
