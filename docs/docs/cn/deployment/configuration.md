---
title: 应用配置
description: config.yml、环境变量，以及如何查看应用实际解析到的配置。
---

# 应用配置

:::warning 文档编写中
本页内容正在编写。
:::

`config.yml` 是配置的唯一来源，创建应用时生成，之后由你维护。它带有 `auth.secret`，因此默认不纳入版本管理。

本页将覆盖配置的组成、环境变量的映射关系，以及如何查看应用最终解析到的配置。
