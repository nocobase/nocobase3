---
title: 开始
description: 创建你的第一个 NocoBase 3 应用，了解它是什么、由什么组成，并把第一个功能走通。
---

# 开始

:::warning 文档编写中
本节内容正在编写。
:::

## 本节将覆盖

- **NocoBase 3 是什么** —— 它生成的是一份归你所有的应用源码，而不是一个待配置的产品
- **创建应用** —— `pnpm create @nocobase/app`，选择数据库，启动开发服务器
- **项目结构** —— `client/`、`server/`、`database/` 各放什么
- **第一个功能** —— 建表、写接口、加页面，完整走一遍
