---
title: 项目结构
description: client、server、database 各放什么，以及三个 composition root 如何决定应用由什么组成。
---

# 项目结构

:::warning 文档编写中
本页内容正在编写。
:::

本页将说明生成出来的目录结构，重点是三个 composition root——`runtime.ts`、`plugins.ts`、`routes.ts`——读这三个文件就知道应用由什么组成、按什么顺序加载。
