---
title: 数据库迁移
description: 用迁移记录表结构变更，用种子数据准备应用必需的初始数据。
---

# 数据库迁移

:::warning 文档编写中
本页内容正在编写。
:::

迁移是不可变的历史记录，必须自包含——每个字段、索引、约束都要在迁移里写清楚，不能引用会继续演进的定义。种子数据只写数据，不建结构。

本页将覆盖迁移的编写、`up` 与 `down` 的对应关系，以及迁移与种子数据的分工。
